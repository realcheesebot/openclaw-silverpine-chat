[Service]
# This host's /tmp cleanup has repeatedly detached PrivateTmp mount namespaces,
# which also makes the ReadWritePaths SQLite bind invisible to the live service.
# Chat does not store application data or secrets in /tmp.
PrivateTmp=false
